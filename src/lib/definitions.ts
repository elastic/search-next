export type IndexType = {
  id: 'string'
  name: 'string',
  type: 'string',
  health: boolean,
  connected: boolean,
  docsCount: number,
}

export type SplitPanelType = {
  title: String,
  summary: String,
  extraSummary: React.ReactNode,
}

